# 作業ルール

- 作業開始前に `origin/main` の最新状態を取得する。
- ユーザーから依頼された箇所だけを変更する。
- プルリクエストや別ブランチを作成しない。
- 変更後は、HTML・CSS・リンクなどに明らかな問題がないか確認する。
- 確認に成功したら、変更を現在の `main` ブランチへコミットする。
- そのまま `origin/main` へ直接 push する。
- 通常のウェブサイト更新では、コミットや push 前の確認をユーザーに求めない。
- 検証に失敗した場合は push せず、問題を日本語で説明する。
- パスワード、トークン、認証情報をファイルへ保存しない。
- ユーザーが明確に依頼していないページ削除や大規模な構成変更は行わない。
