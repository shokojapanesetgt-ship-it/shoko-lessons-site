# Shoko's Japanese Lessons — 上线部署交接文档

给新对话的上下文说明。静态网站（HTML/CSS/JS，无框架，无构建步骤），4 个页面已完成 Figma 像素级还原 + 响应式审计（320px～1512px+ 均验证无横向溢出），首页 index.html 还额外做过一轮手机端精修。用户现在想解决**网站上线部署**的问题。

## 文件位置

- `site/index.html` — 首页
- `site/booking.html` — 预约入口页（两张选择卡片）
- `site/booking-code.html` — 老学生输入预约码页
- `site/booking-new.html` — 新学生流程说明页（What happens next）
- `site/assets/styles.css` — 共用样式表
- `site/assets/*` — 图片/SVG 素材
- `site/handoff-responsive.md` — 上一轮响应式工作的交接文档（背景资料，可参考里面记录的踩坑经验）

网站是纯静态文件，没有后端、没有数据库、没有构建工具（不用 npm run build 之类的步骤），部署本质上就是把这个文件夹原样传到静态托管平台上。

## 响应式工作完成情况

- **index.html**：完整走过一遍手机端精修（375～430px 真机宽度），包括：导航栏改汉堡菜单、hero 区文字居中、各 section 间距收紧、Recommended for 区块用 CSS Grid 重排（手机端顺序：标题→视频→列表，桌面端不受影响）、评价卡片取消固定高度、底部 Logo+社交图标一行/邮箱+地址单列、touch target 加大等。这一页手机端体验已经比较打磨过。
- **booking.html**：只做了两处针对性修复——"Not sure which to choose?" 文字块居中，以及补上了跟首页一样的汉堡菜单（发现这页的导航栏因为共用 styles.css，被首页那次改动意外隐藏、之前完全打不开，已修复）。**没有**像首页那样做整体间距/细节精修。
- **booking-code.html / booking-new.html**：还没有专门做手机端精修审计，目前只验证过"不出硬伤"（不溢出、不重叠）那一轮。这两页头部是简化版（只有 Logo + "Back to booking" 链接，没有导航菜单，所以不受汉堡菜单那个 bug 影响）。

如果用户接下来想继续精修这三页，可以按 `handoff-responsive.md` 里的方法论继续；但当前对话的任务是**上线部署**，除非用户主动提，不用主动去改这几页的样式。

## ⚠️ 上线前需要用户确认/注意的点

1. **`booking-code.html` 的验证码校验是硬编码的测试逻辑**（第 116-119 行左右）：
   ```js
   var BOOKING_URL = "https://calendar.app.google/iD777ugPei6K3zU29";
   var VALID_CODE = "1a2b3c"; // Test code for trying the flow out. Swap for a real lookup later.
   ```
   任何人输入 `1a2b3c` 都能通过验证并跳转到 Google Calendar 预约链接。这是纯前端写死的占位逻辑，**不是真实的老学生验证码系统**，代码里也明确写了注释"以后要换成真的查询"。如果直接原样上线，等于没有真正的门槛。上线前一定要跟用户确认：这个验证码校验打算怎么处理？（比如：先手动一对一发送同一个码给所有老学生？做一个真实的码库？还是先不管，等有实际需求再做？）这个跟"部署到哪个平台"是两件独立的事，但直接影响能不能安心上线，建议优先跟用户对齐。

2. **`booking-new.html` 里"Fill out the form"按钮**链接到一个 Google 表单（`docs.google.com/forms/...`），**`booking-code.html`** 里的预约链接指向一个 Google Calendar 预约页（`calendar.app.google/...`）。上线前值得跟用户确认这两个链接是不是最终要用的正式版本（不是某个测试用的表单/日历）。

3. **Google Calendar 时区问题**（免费版无法按访客时区自动换算）是已知问题，用户之前的态度是"先不管"——不用主动提，除非用户自己问起或者部署过程中恰好碰到需要处理的地方。

## 部署平台

用户之前对比过 **Netlify / Cloudflare Pages / GitHub Pages**，倾向用**免费子域名**，还没有最终拍板选哪个平台。三个平台对这种纯静态站点都能直接支持（拖拽文件夹或连 Git 仓库，零配置），核心差异一般在：免费额度、自定义域名/HTTPS 是否顺手、部署方式（拖拽 vs Git 自动化）、后续如果加日记/CMS 功能时的扩展性。可以在新对话里帮用户过一遍这几个平台的当前实际情况（免费额度、限制等可能会变，建议上网查最新信息而不是凭记忆判断），然后根据用户的技术熟悉程度和后续需求给建议。

## 沟通习惯 / 用户偏好（重要）

- 回复要简洁直接，不要长篇大论、不要过度使用列表/加粗。
- 对于非平凡的改动，习惯是"先讨论方案，用户确认后再动手"；明确的小调整可以直接执行。
- 用户是设计师本人，注重细节和实际效果，不只是"能跑起来"就算完成。
- 涉及"这两天/上线之后网站会不会崩、能不能长期稳定跑"这类问题，如实说明，不要过度承诺。

## 暂缓的其他事项（不要主动提起，除非用户重新提）

1. 客户想加的日记/vlog 功能（三个方向：静态页面、接 Notion/CMS、链接到现有社交媒体，用户想先规划思路，还没深入）——如果这个功能有 CMS/后端需求，会反过来影响部署平台的选择（比如需要 serverless function 或跟 CMS 对接），但目前还没到这一步，不用主动带出来。
